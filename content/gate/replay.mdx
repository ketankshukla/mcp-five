---
title: "Replay"
emoji: "⏪"
summary: "Every loop event was already a row, so rewinding a run costs one `order by seq`."
project: 3
---

## Coming in a later milestone

This page is a stub. The chrome around it is real: the sidebar, the pager and
the on-this-page rail all come from `lib/navigation.ts`, and this file is
`content/gate/replay.mdx`.

### What will be here

Every loop event was already a row, so rewinding a run costs one `order by seq`.
