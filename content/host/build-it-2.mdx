---
title: "Build it"
emoji: "🔨"
summary: "Twelve stages, their checkpoints, and the VERCEL_URL trap."
project: 2
---

The full walkthrough is
[`BUILD_FROM_SCRATCH.md`](https://github.com/ketankshukla/learn-mcp-agent-loop/blob/main/BUILD_FROM_SCRATCH.md).
This is the shape, and the seven things that broke.

<Steps>
  <Step n="0–1" title="Prerequisites, scaffold and install">
    An `ANTHROPIC_API_KEY` this time — project #2 is the first one that costs
    money to run.

    Same scaffolding gotcha as project #1: `create-next-app` refuses a
    non-empty folder. Temp dir, then copy in.
  </Step>

  <Step n={2} title="Read the types before you write any code">
    The habit that changed the design in all five projects.

    <Checkpoint>
      You can state, from memory, the exact signature of `registerTool` and
      whether your model accepts `temperature`.
    </Checkpoint>
  </Step>

  <Step n={3} title="The MCP client">
    About two hundred lines of `fetch` — see [Writing the client by
    hand](/learn/host/client-by-hand).

    <Checkpoint>
      Run it against the **live** project #1 server, not a local one:

      ```bash
      npm run mcp:list
      ```
    </Checkpoint>
  </Step>

  <Step n={4} title="Schema translation">
    `inputSchema` → `input_schema`, strip `$schema`, substitute an empty object
    for a missing one.

    <Checkpoint>
      ```bash
      npm run mcp:translate
      ```
    </Checkpoint>
  </Step>

  <Step n={5} title="The agent loop (non-streaming)">
    Get it right before making it pretty.

    <Checkpoint>
      The demo, in a terminal, before any React exists:

      ```bash
      npm run agent
      ```

      Two tool calls across two iterations, with the second one's `count`
      matching the first one's total.
    </Checkpoint>
  </Step>

  <Step n="6–7" title="The route handler and the chat UI">
    The key is read in exactly one place. The browser sends plain text and
    receives an NDJSON stream of loop events.

    <Checkpoint>
      ```bash
      curl -N -X POST http://localhost:3000/api/chat \
        -H "Content-Type: application/json" \
        -d '{"messages":[{"role":"user","content":"roll one d6"}]}'
      ```
    </Checkpoint>
  </Step>

  <Step n={8} title="The second MCP server">
    In this repo, at `/api/toolbox` — and with a deliberate `secret_code`
    collision, so [namespacing](/learn/host/the-toolbox) has something to
    prove itself against.
  </Step>

  <Step n={9} title="Auth on your own server">
    `withMcpAuth` and a shared token.

    <Checkpoint>
      With `MCP_SHARED_TOKEN` set and the dev server **restarted**: no token
      gives 401, the right token gives 200.
    </Checkpoint>

    <Callout type="warning" title="This stage is why the next two projects had a bad time">
      It works exactly as designed. It also locked project #3 and project #4
      out of this server, and neither of them noticed until they had already
      architected around reaching it.

      Project #4 reversed the default and shipped its servers **open**, on
      purpose. Project #5 got three reachable servers with zero setup as a
      direct result.
    </Callout>
  </Step>

  <Step n="10–12" title="Typecheck, build, GitHub, deploy">
    ```bash
    npx tsc --noEmit && npx eslint . && npm run build
    ```

    <Checkpoint>
      Verify against **production**, not localhost.
    </Checkpoint>
  </Step>
</Steps>

## The gotcha worth the whole page

<Callout type="gotcha" title="VERCEL_URL is the per-deployment hostname, and Deployment Protection answers it with 401">
  The host needs to call its own in-repo MCP server. The obvious way to build
  that URL is `process.env.VERCEL_URL`.

  That variable holds the **immutable per-deployment** hostname — something
  like `myapp-4kd91xz-team.vercel.app` — and with Deployment Protection turned
  on, that hostname requires authentication. So your app calls itself, gets a
  `401`, and reports that its own MCP server is unreachable.

  Use **`VERCEL_PROJECT_PRODUCTION_URL`** when the host must call itself. That
  is the stable production hostname, and it is not behind the protection
  check.
</Callout>

The reason this one is nasty: it works perfectly in development, works
perfectly in preview if protection happens to be off, and fails only in the
configuration you ship. And the symptom — "my MCP server is down" — points at
the server, which is fine.

## The other six

| # | What broke | Where it's covered |
|---|---|---|
| 1 | `create-next-app` refuses to share a folder | [Build it, project #1](/learn/server/build-it-1) |
| 2 | The MCP package that isn't the one you installed | [Build it, project #1](/learn/server/build-it-1) |
| 3 | The type files you can't find | [the compendium](/learn/picture/gotchas) |
| 4 | `.env*` swallows `.env.example` | below |
| 5 | The dev server that would not die | below |
| 6 | `.env.local` is read at startup, not per request | below |
| 7 | The host called itself through a locked door | above |

**`.env*` swallows `.env.example`.** The Next.js `.gitignore` ships with
`.env*`, which also matches the example file you actually want committed. Add
`!.env.example` and keep that block **last**, because a later pattern wins.

**The dev server that would not die.** Killing the shell that started it does
not kill it. Check the **port**, not the shell:

```bash
Get-NetTCPConnection -LocalPort 3000 -State Listen
```

This one bit project #3 and project #4 as well. Three projects, same
surprise — which is a good sign the mental model is wrong rather than the
command.

**`.env.local` is read at startup, not per request.** Add a variable, and the
running dev server does not know about it. Restart, then re-test — otherwise
you will "fix" a working config three times.
